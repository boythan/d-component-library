/// <reference types="react" />
export interface InputTextProps {
    [key: string]: any;
}
declare const InputText: ({ className, classNameInput, variant, multiple, value, error, name, label, key, placeholder, type, rows, cols, disabled, onChange, onBlur, }: InputTextProps) => JSX.Element;
export default InputText;
