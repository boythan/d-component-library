/// <reference types="react" />
export interface InputTextProps {
    [key: string]: any;
}
declare const InputTextSearch: ({ className, variant, value, error, name, key, placeholder, disabled, onChange, onBlur, }: InputTextProps) => JSX.Element;
export default InputTextSearch;
