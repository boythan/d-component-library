/**
 * Specify all file types which is accepted by the html input tag  =>
 * get to know more @param {*} https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/file#unique_file_type_specifiers
 * @param {*}
 * @returns
 */

export enum FileExtensionType {
    PDF = ".pdf",
    DOC = ".doc",
    JPG = ".jpg",
}

export interface IFileInputType {}
