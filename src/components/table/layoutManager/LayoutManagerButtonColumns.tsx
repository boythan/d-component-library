import React from "react";
import Button from "../../button/Button";

const LayoutManagerButtonColumns = () => {
    return (
        <div>
            <Button
                content="Column"
                iconName="settings"
                variant="trans"
                onClick={() => {}}
                color="gray"
                className="font-weight-normal"
            />
        </div>
    );
};

export default LayoutManagerButtonColumns;
